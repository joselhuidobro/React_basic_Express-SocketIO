const app = require('express')();

const server = require('http').createServer(app);

const io = require('socket.io')(server);

io.on('connection', () => { 
 cosole.log('Cliente conectado');

 });



server.listen(8080,() => {
    console.log('server corriendo en puerto: 8080');
});
